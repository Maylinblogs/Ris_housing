import {useEffect, useState} from "react";
import executeRequest from "./../sender.js"

export default function useCertificateSearch(searchName, searchParams, searchTags, pageNumber, counter) {


    //[searchName, searchParams, searchTags, pageNumber, counter]

    return {loading, empty, content, hasMore}
}